# Angular 8 with Docker Boilerplate

## Docker Build

Run `docker build -t dockerangular .`

## Docker Run

Run `docker run -it -p 8000:80 --name dockerangular dockerangular`

Navigate to `http://localhost:8000/`